// creates a variable called 'likecount' and assigns it a value of ZERO
var likeCount = 0;
// creates a function called 'increaseLikes'
function increaseLikes() {
    // tells the variable to add 1 to its current value
    likeCount = likeCount + 1;
}
